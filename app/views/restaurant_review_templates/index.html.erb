<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All restaurant reviews
    </h1>

    <a href="/restaurant_reviews/new" class="btn btn-block btn-outline-secondary">
      Add a new restaurant review
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Restaurant
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @restaurant_reviews.each do |restaurant_review| %>
      <tr>
        <!-- Display restaurant_review.id start -->
        <td>
          <%= restaurant_review.id %>
        </td>
        <!-- Display restaurant_review.id end -->

        <!-- Display restaurant_review.restaurant_id start -->
        <td>
          <% if restaurant_review.restaurant.present? %>
            <a href="/restaurants/<%= restaurant_review.restaurant.id %>">
              <%= restaurant_review.restaurant.restaurant_name %>
            </a>
          <% end %>
        </td>
        <!-- Display restaurant_review.restaurant_id end -->

        <!-- Display restaurant_review.user_id start -->
        <td>
          <% if restaurant_review.user.present? %>
            <a href="/users/<%= restaurant_review.user.id %>">
              <%= restaurant_review.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display restaurant_review.user_id end -->

        <!-- Display restaurant_review.created_at start -->
        <td>
          <%= time_ago_in_words(restaurant_review.created_at) %> ago
        </td>
        <!-- Display restaurant_review.created_at end -->

        <!-- Display restaurant_review.updated_at start -->
        <td>
          <%= time_ago_in_words(restaurant_review.updated_at) %> ago
        </td>
        <!-- Display restaurant_review.updated_at end -->

        <td>
          <a href="/restaurant_reviews/<%= restaurant_review.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @restaurant_reviews, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
